var color = 'blue';
<% if !@success %>
	color = 'red';
<% end %>
Materialize.toast("<%= j @message %>", 4000, color);
$('.tooltipped').tooltip('remove');
$('#content').html("<%= j (render 'ingredients') %>");
$('.tooltipped').tooltip({delay: 50});
$('.collapsible').collapsible({
      accordion : false
});